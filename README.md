# AI-image-Generator
为了方便调用GPT image-2和2.5的生成图工具，只需要填写对应的api_key和接口地址即可调用image模型了，去除了繁琐的终端模式。

我已经将python 代码打包成了exe程序，只要运行exe程序即可使用软件。

1.打开软件后，输入对应的api_key和对应的接口地址，选择模型后保存，即可使用程序。\
2.注意提示词需要具体些，越详细生图越好，最好有场景、风格、主体、形象等参数，可以先去对话模型中把这个信息收集出来。\
3.因为github不能上传大于25m的文件，所以我将打包好的exe程序放到了蓝奏云\
